- add service for menu
- Internationalisation
- Unit tests
- ES6?
- add template in another template, for example, use a partial in page1
- consider using link in directive for smooth scrolling

-> test: {
	directives: {
		filtering,
		create element,
		create attribute
	},
}

-> blog: {
	config
	nested UI-Views with view@parent
	scroll smooth with angular
	add js files to body 
	add css files to head
	service for menu
	gulp
	internationalization
}
